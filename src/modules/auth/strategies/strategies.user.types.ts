export type GoogleUser = {
  email: string,
  firstName: string,
  lastName: string,
  picture: string,
  accessToken: string,
  refreshToken: string,
}

export type GithubUser = {
  email: string,
  displayName: string,
  username: string,
  picture: string,
  accessToken: string,
  refreshToken: string,
}
